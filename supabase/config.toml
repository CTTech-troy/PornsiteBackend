# Supabase CLI — https://supabase.com/docs/guides/cli/config
# Deploy: supabase db push && supabase functions deploy

[api]
enabled = true

[functions]
# Firebase ID tokens verified in code; do not use Supabase JWT gate on these functions
verify_jwt = false
